# RoordaWrapped

Interactive “Wrapped”-style experience for Roorda.

## Live
https://<jouw-netlify-url>

## Structure
- `index.html` – main markup
- `wrapped.css` – styling & animations
- `wrapped.js` – interaction & flow

## Deploy
Automatically deployed via Netlify from `main`.
